#version 330 core

layout(location = 0) in vec4 vertexPosition_modelspace;

void main()
{
  gl_Position = vertexPosition_modelspace;
  
  gl_Position *= 0.5;

  gl_Position.z = 0;

  gl_Position.w = 1.0;
}
